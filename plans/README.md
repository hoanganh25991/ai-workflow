# Plans

Plan artifacts from `/plan` are saved here as `plan-<ID>.md` (ID = `plan-YYYYMMDD-HHMMSS`).

To implement a specific plan:

```
/execute plans/plan-<ID>
```

Example: `/execute plans/plan-20260319-103000`
